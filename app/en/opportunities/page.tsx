import Link from "next/link"; import { listPublicOpportunities } from "@/lib/opportunities/repository";
export const dynamic="force-dynamic";
export default async function EnglishOpportunitiesPage(){let items:any[]=[];try{items=await listPublicOpportunities()}catch(error){console.error("[en opportunities] query failed",error)} return <main style={{maxWidth:1100,margin:"0 auto",padding:"72px 24px"}}><p>POPOK OPPORTUNITIES</p><h1>Opportunities in the performing arts</h1><p>Original notices are shown without automatic translation.</p>{items.length?<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))",gap:20}}>{items.map(item=><article key={item.id} style={{borderTop:"2px solid #111",paddingTop:16}}><small>{item.opportunity_type}</small><h2>{item.title}</h2><p>{item.organization}</p><Link href={`/opportunities/${item.id}`}>View details</Link></article>)}</div>:<p>No verified opportunities are available yet.</p>}</main>}
