import { notFound } from "next/navigation"; import { getPublicOpportunity } from "@/lib/opportunities/repository";
export const dynamic="force-dynamic";
export default async function OpportunityDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params; let item:any=null; try{item=await getPublicOpportunity(id)}catch(error){console.error("[opportunity detail] query failed",error)} if(!item)notFound(); const href=item.original_publisher_url||item.application_url||item.source_url;
return <main style={{maxWidth:900,margin:"0 auto",padding:"72px 24px"}}><p style={{color:"#1649d8",fontWeight:700}}>{item.opportunity_type}</p><h1>{item.title}</h1><p>{item.organization}</p><dl><dt>지역</dt><dd>{item.region||"미정"}</dd><dt>마감</dt><dd>{item.deadline?new Date(item.deadline).toLocaleString("ko-KR",{timeZone:"Asia/Seoul"}):"일정 미정"}</dd></dl>{item.summary&&<p>{item.summary}</p>}{item.description&&<div style={{whiteSpace:"pre-wrap"}}>{item.description}</div>}<p><a href={href} target="_blank" rel="noopener noreferrer">원문 또는 지원 페이지 보기</a></p></main>}
